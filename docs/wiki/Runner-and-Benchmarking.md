# Runner and Benchmarking

## Python runner

Use `runner/run_problem.py` for local pass/fail + runtime summary.

## Shell runner

Use `runner/run_shell_problem.py` for command-output validation on public fixtures.

## Metric labels

- `strict`: enforced against explicit runtime limits
- `advisory`: indicative and environment-dependent
