# Binary Tree Level Order Traversal

## Description

Given a binary tree represented as an array in level-order form (using `null` for missing nodes), return the node values grouped by depth level.

## Input

- `values`: list where children of index `i` are at `2*i+1` and `2*i+2`

## Output

- list of levels, each level is a list of node values
