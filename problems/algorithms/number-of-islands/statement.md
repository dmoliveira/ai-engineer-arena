# Number of Islands

## Description

Given a 2D grid of `'1'` (land) and `'0'` (water), return the number of islands. An island is formed by connecting adjacent lands horizontally or vertically.

## Input

- `grid`: list of list of string values `'0'` or `'1'`

## Output

- integer count of islands
