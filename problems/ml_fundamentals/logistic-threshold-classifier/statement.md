# Logistic Threshold Classifier

## Description

Given a list of model probabilities and a threshold, convert each probability into class label `1` if `p >= threshold`, else `0`.

## Input

- `probabilities`: list of floats
- `threshold`: float

## Output

- list of integer labels
